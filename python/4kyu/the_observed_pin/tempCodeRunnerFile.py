print(pin_variations)
    # pin_variations *= len(digit_variations[int(observed[1])])
    # print(pin_variations)
    # for i in range(0, len(pin_variations)):
    #     j = i // len(digit_variations[int(observed[1])])
    #     print(j)
    #     pin_variations[i] += digit_variations[int(observed[1])][j]